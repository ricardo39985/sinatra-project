<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/stylesheets/main.css" />  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Here in my Garage</title>
</head>
<div class="container">
  <div class="card grey">
    <div class="card-content white-text">
        <h2><%=@user.username.capitalize%>, Welcome to your Garage</h2>
    </div>
  </div>
</div>
  <% if @user.cars.size==0 %>
  <div class="container">
    <div class="card grey">
      <div class="card-content white-text">
        <p class = "flow-text">You currently have no cars. Why not <a href="/<%=current_user.id%>/create_car">Add a car</a> </p>
      </div>
    </div>
  </div>

  <% else %>
  <div class="viewall">
    <% @user.cars.each do |car| %>
      <div class="container">
        <div class="card grey hoverable z-depth-1">
            <div class="card-content white-text">
              <div class="valign-wrapper">
                <h5 id="viewall"><%=car.make.capitalize%> <%=car.model.capitalize%>, <%=car.year%></h5>
              </div>
            </div>
        </div>
      </div>
    <% end %>
  </div>
  <% end %>
</div>
<div style="height:245px"></div>


